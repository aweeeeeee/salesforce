public with sharing class StaticAndInstance {
    public void DisplayText(String stringToDisplay) {
        System.debug('display text' + stringToDisplay);
    }
    // StaticAndInstance a = new StaticAndInstance();
    // a.DisplayText('awe');
}
