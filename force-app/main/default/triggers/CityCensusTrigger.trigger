trigger CityCensusTrigger on City_Census__c (before insert, after insert,
                                             before update, after update) {

    //CityCensusTriggerHandler.CreateHandler();

}